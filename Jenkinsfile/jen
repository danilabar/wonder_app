// Declarative //
pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
    stages {
        stage("checkout") {
            steps {
                checkout scm
            }
        }
    }
}
