// Declarative //
pipeline {
    agent any
    triggers {
        cron(' * * * * *')
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
        stage("checkout") {
            steps {
                checkout scm
                }
            }
        }
    }
}